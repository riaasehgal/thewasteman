# Server
PORT=3001

# SQLite database path (relative to backend/)
DB_PATH=./data/trashtrack.sqlite

# Staff credentials (Basic Auth for dashboard access)
STAFF_USER=staff
STAFF_PASS=changeme

# Device secret (shared secret header for ingestion API)
DEVICE_SECRET=device-secret-changeme
